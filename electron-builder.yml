appId: com.electron.yab
productName: yab
copyright: hmm
win:
  target: portable
  artifactName: ${productName}-v${version}-${os}_${arch}.${ext}
directories:
  output: dist
  buildResources: build
  app: .
asar: true
publish:
  provider: github
  releaseType: release